# Signaling Server Configuration

# Required: Password for client authentication
# Clients must provide this in the Sec-WebSocket-Protocol header
SIGNALING_PASSWORD=change-me-in-production

# Optional: Server port (default: 8000)
# Note: Deno Deploy ignores this and assigns a port automatically
PORT=8000
